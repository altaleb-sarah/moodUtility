# MoodUtility
A simple Java utility tool to analyze user mood from text input.
A utility class used to analyze the user's mood based on text input. It checks for basic positive and negative keywords and returns "POSITIVE", "NEGATIVE", or "NEUTRAL" accordingly.

